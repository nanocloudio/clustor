# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store

# AI
.claude/
.context/
.codex

# Build/test
Cargo.lock
/target/

# Shadow-tracked (see standards/test-tracking.md) — versioned in
# .git-shadow/, never pushed to this repo's remote.
/tests/
/benches/
/configs/
/.git-shadow/

# Runtime WAL state (mutated by local runs, not source)
/wal/

# fluxor lockfile advisory-lock artifact
/.fluxor.lock.lock
